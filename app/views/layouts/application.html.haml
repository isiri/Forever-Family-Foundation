= render 'layouts/header'

%body
  #layout

    #container

      #sidebar
        = render 'shared/nav/user_nav'

        = render 'shared/nav/site_nav'
            
      #content

        = render 'shared/nav/global_nav'
        
        #content_main
          %p.notice
            = notice
          %p.alert
            = alert

          %h1= @cms_page.title
          = textilize @cms_page.body
          = yield

    #footer_spacer
  = render 'layouts/footer'